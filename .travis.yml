sudo: false
language: node_js

after_script: mocha --require blanket -R mocha-lcov-reporter | coveralls
cache:
  directories: node_modules

notifications:
  irc:
    nick: gulp-slm
    channels: 'ircs://irc.uriirc.org:16667#hyeon'
    template: '#%{build_number} %{result}, %{build_url}'
    skip_join: true
